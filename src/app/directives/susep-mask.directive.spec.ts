import { SusepMaskDirective } from './susep-mask.directive';

describe('SusepMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new SusepMaskDirective();
    expect(directive).toBeTruthy();
  });
});
