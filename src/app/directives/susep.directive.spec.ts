import { SusepDirective } from './susep.directive';

describe('SusepDirective', () => {
  it('should create an instance', () => {
    const directive = new SusepDirective();
    expect(directive).toBeTruthy();
  });
});
