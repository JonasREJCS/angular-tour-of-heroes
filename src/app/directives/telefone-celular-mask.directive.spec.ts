import { TelefoneCelularMaskDirective } from './telefone-celular-mask.directive';

describe('TelefoneCelularMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefoneCelularMaskDirective();
    expect(directive).toBeTruthy();
  });
});
